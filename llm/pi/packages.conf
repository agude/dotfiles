# Pi packages installed by `just bootstrap-pi`.
#
# Keep versions pinned. `pi install` records these packages in Pi's mutable
# settings file, so normal dotfile installation remains network-free.
npm:pi-web-access@0.28.0
